import { Meteor } from 'meteor/meteor';

// Collections
import '../imports/api/boxes/boxes.js';
import '../imports/api/comments/comments.js';

Meteor.startup(() => {
	// code to run on server at startup
});
